import { Property } from '../registry.mjs';
class FoodProperty extends Property {}
describe('Property Specifiction Test: ', () => {
    describe(`when creating a property given property name and type`, () => {
        beforeAll(() => {
            new FoodProperty()
        });
        it('should ', () => {
        
        });
    });
});